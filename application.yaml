eureka:
  client:
    service-url:
      defaultZone: http://vm-node-a.platform:8080/eureka/,http://vm-node-b.platform:8080/eureka/,http://vm-node-c.platform:8080/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    lk.ijse.gdse: DEBUG
    org.springframework.web: INFO
    org.hibernate.sql: DEBUG
